# AGENTS

정적 웨딩 준비 사이트(HTML/CSS/바닐라 JS). 백엔드·외부 API 없음.

작업 전 내부 JS 모듈 API·데이터 스키마·localStorage 규칙을 정리한 노트를 먼저 읽는다:
`.claude/skills/wedding-site-api/SKILL.md`

특히 `calendar.js`/`timeline.js`/`budget.js`/`glossary.js`, `data/*.js`,
`assets/js/{storage,shared-id,share-image}.js`를 고치기 전에 참조할 것.
